"""Legacy compatibility modules kept outside ``src/``.

These modules preserve the old import surface for notebooks or ad-hoc scripts,
while the canonical project implementation now lives under ``src/``.
"""
